# Base URL of the ProjectSphere backend API.
# Cross-origin requests send the httpOnly refresh cookie (backend CORS allows
# credentials for http://localhost:5173).
VITE_API_BASE_URL=http://localhost:5000/api/v1
